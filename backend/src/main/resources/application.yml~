spring:
  application:
    name: wordsmith
  datasource:
    url: jdbc:postgresql://localhost:5432/wordsmith
    username: postgres
    password: postgres
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

server:
  port: 8080
  shutdown: graceful

jwt:
  secret: 
  expiration: 900000